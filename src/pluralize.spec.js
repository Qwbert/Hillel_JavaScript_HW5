/* global require, test, expect */
const { triangle } = require("./i");

test("Symbole", () => {
  // Loop should start here
  expect(triangle(i)).toBe(">=1 && <= 1000"); // this inside loop
  // Loop should end here
});


